@startuml
title Test #14: op throw 时向上抛（不写入 outputs）

object "Before" as b {
  state.stack:
  (empty)
  state.internalStore:
  (empty)
  state.publicStore:
  (empty)
}

object "After" as a {
  state.stack:
  (empty)
  state.internalStore:
  (empty)
  state.publicStore:
  (empty)
}

b -[#red]right-> a : ✗ Error thrown
note right of b : entry.status: pending\n(unchanged — failure path keeps stack)
note right of a
  **验证**
  rejects.toThrow(/hard error/)
end note

@enduml
