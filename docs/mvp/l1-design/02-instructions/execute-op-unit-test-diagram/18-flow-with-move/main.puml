@startuml
title Test #18: 完整流程：literal 5 → move → $r0 → mock_op → $r1

object "Before\nstate.internalStore" as int_b {
  $r0 = 5
}
object "After\nstate.internalStore" as int_a {
  $rr1 = 10
}
object "Before\nstate.publicStore" as pub_b {
  (empty)
}
object "After\nstate.publicStore" as pub_a {
  (empty)
}

int_b ..> int_a : execute(entry, state)
pub_b ..> pub_a : (业务数据持久化)


note right of int_a
  entry.status: pending → done
  **验证**
  $r1 === 10
end note

@enduml
