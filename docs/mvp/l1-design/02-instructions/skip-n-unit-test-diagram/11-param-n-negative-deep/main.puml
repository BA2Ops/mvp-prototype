@startuml
title Test #11: n=-100 抛 SkipNError

object "Before" as b {
  state.stack:
  (empty)
  state.internalStore:
  (empty)
  state.publicStore:
  (empty)
}

object "After" as a {
  state.stack:
  (empty)
  state.internalStore:
  (empty)
  state.publicStore:
  (empty)
}

b -[#red]right-> a : ✗ SkipNError thrown
note right of b : entry.status: pending\n(unchanged — failure path keeps stack)
note right of a
  **验证**
  expect(executeSkipN(skip, state)).rejects.toThrow(SkipNError)
end note

@enduml
