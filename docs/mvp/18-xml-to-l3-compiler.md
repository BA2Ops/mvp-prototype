# 18 - XML → L3 编译器：推理补全（能力定义）

> **状态**：能力定义与预期表达（设计前讨论稿）。
> 关联：[15 可视化与 XML 中间定义](./15-experience-visualization.md)（输入来源）、
> [16 LLM 辅助设计](./16-experience-llm-assisted-design.md)（XML 的编辑者）、
> [17 元数据规范](./17-experience-metadata.md)、
> [06 执行层](./06-execution-layer.md)（L3 编译器/L2 契约）、
> [09 L1 实现](./09-l1-implementation.md)（双区约束）。

---

## 决策摘要

本模块的能力定义：**推理补全器**，而非格式转换器。

> **XML 中仅收集用户可提供的信息。** 编译器基于用户提供的信息，
> 以及系统自身能力、设计哲学和逻辑、系统 L3/L2/L1 的实际实现和能力，
> **推理补全所有其他信息**，生成完整可执行的 L3 Experience 定义。

用户与编译器的分工：

| | 用户（经 XML）| 编译器（推理补全）|
|---|---|---|
| 表达什么 | 业务意图：做什么、顺序、数据从哪来到哪去、分支含义 | 一切执行机制 |
| 需要知道 | 节点类型 + 参数 + 关系 | 系统全部实现细节 |

---

## 一、推理补全的知识来源（编译器的「系统知识库」）

编译器不是猜测——所有补全都基于以下机器可读的系统知识：

| 知识来源 | 内容 | 补全示例 |
|---|---|---|
| **L2 op 注册表（formalSpec）** | 每个 op 的输入输出契约、错误语义 | 「读取文件」节点 → file_read op + content/error 输出绑定 |
| **节点类型目录** | 业务节点类型 → op 模板的映射、参数契约 | 「替换内容」类型 → string_replace + replace_all 默认 |
| **L3 编译既有规则** | 输入绑定、分支构造、sidecar 装载、路径标记 | 参数连接 → 寄存器分配与 move（用户不可见）|
| **设计哲学（文档化规则）** | 错误数据化 / handleError 处置权 / 信息返回权 / 双区约束 | 未声明错误处理的获取节点 → 按哲学补全 $r_err 绑定与 failure 默认 |
| **L1 约束** | execute_op 只读写 internal、literal 需 sidecar | 常量参数 → 自动 sidecar move |
| **经验库既有经验** | 相似结构的既有范例 | 回复消息风格、错误处理模式的一致性 |

---

## 二、推理补全的类别（用户信息 vs 补全信息对照）

| 用户在 XML 中提供 | 编译器推理补全 |
|---|---|
| 节点及其业务类型、参数内容 | 对应 L2 op、formalSpec 绑定、sidecar 装载 |
| 顺序界定 | pre_processing 归属 / target steps 依赖序 |
| 参数连接（A.x → B.y）| 寄存器分配、inputs/outputs 地址绑定 |
| 字面量参数值 | sidecar move 装载 |
| 判断节点的业务问题 | 条件表达式（evaluate_expr AST）+ 分支构造（cskip/skip 计算）|
| 汇聚参数意图 | 各分支落点与收尾绑定 |
| 受控回环 | 递归自嵌套结构 + 终止 judgment |
| 结果消息（可选提供）| 未提供时按节点类型/哲学生成默认模板 |
| 错误处理意图（可选提供）| 未提供时按设计哲学补全（数据化 + handleError 默认）|

**原则**：用户提供的越少，编译器补全的越多——但补全永远基于上表
知识来源，**确定性、可解释、可复现**（同输入同输出，无随机性）。

---

## 三、预期行为表达（示例）

> 描述性示例，非实现规格。

- 用户只画了一个「读取文件」节点（路径参数来自输入）
  → 预期产出完整 L3：file_read 步骤、content/error 输出绑定、
  ENOENT 失败消息模板——**用户一个字都没提错误处理**，
  系统哲学自动补全。
- 用户画判断「文件已存在？」→ 预期编译为基于 file_read 错误语义的
  条件表达式（具体表达式形式用户不可见），并生成完整分支骨架。
- 用户提供的两条信息存在冲突（如顺序界定成环且无终止判断）
  → 预期**编译失败并指出需要用户澄清什么**，而非猜测。
- 同一份 XML 重复编译 → 预期产出语义一致（理想情况下逐字节一致）的 L3。

## 四、能力边界

**做**：
- 基于系统知识库推理补全全部执行机制
- 信息完备性校验：无法补全时明确报告缺口（请求用户提供什么）
- 语义等价保证：同一 XML 的多次编译行为一致

**不做**：
- 不做业务语义的猜测（用户没表达的业务意图不会凭空出现）
- 不做非确定性的「智能」补全（本模块是确定性程序；创造性生成
  是 doc 16 LLM 辅助设计的职责，两者边界清晰）

## 五、待明确问题

1. **条件语义 → 表达式的映射规范**（doc 15 遗留的最大难点）：
   「文件已存在？」的推导依据——节点类型目录内置约定？
   还是 XML 允许用户显式指定判断依据？
2. **补全冲突的优先级**：用户显式信息 > 节点类型目录默认 >
   设计哲学默认——这个优先级链是否完备？
3. **编译产物的可追溯性**：是否需要在 L3 中保留「来自 XML 何处」的
   溯源注记（调试用）？与「用户不可见」原则如何共存（注记不进用户视野）？
4. **节点类型目录的治理**：目录本身如何扩展/版本化（它是系统能力的
   业务层暴露面，与 doc 07 意图库的关系需要厘清）。
